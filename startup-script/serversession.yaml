session_name: star-server
windows:
- window_name: dev window
  layout: tiled
  shell_command_before:
    - cd $parentdir
    - mkdir -p $dir/log
    - cd ~/$dir
  panes:
    - bin/bbserver -p $bbport
    - bin/star-keygen -p $keygenport
    - bin/star-voter-db -p $dbport
    - bin/star-controller -p $controllerport
    - 
